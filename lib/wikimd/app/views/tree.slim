- tree.each do |name,m|
  li.entry class=m[:type]
    - if m[:type] == :directory
      span = name
      ul.tree-view == slim :tree, locals: { tree: m[:sub] }
    - else
      a href=url(m[:path]) = name
